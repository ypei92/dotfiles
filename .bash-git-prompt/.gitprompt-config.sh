# first, source the system bashrc
GIT_PROMPT_ONLY_IN_REPO=0

# Fetching remote status
# GIT_PROMPT_FETCH_REMOTE_STATUS=0

# Avoid searching for changed files in submodules
GIT_PROMPT_IGNORE_SUBMODULES=1

# Show upstream tracking branch
GIT_PROMPT_SHOW_UPSTREAM=1

# Can be no, normal or all
GIT_PROMPT_SHOW_UNTRACKED_FILES=no

# Printing the number of changed files
GIT_PROMPT_SHOW_CHANGED_FILES_COUNT=1

# Custom prompt start sequence and end sequence
# GIT_PROMPT_START=...
# GIT_PROMPT_END=...

# Choose the theme
GIT_PROMPT_THEME=Custom
GIT_PROMPT_THEME_FILE=~/.bash-git-prompt/.git-prompt-colors.sh
